buildscript {
    ext {
        buildToolsVersion = "35.0.0"
        minSdkVersion = 24
        compileSdkVersion = 35
        targetSdkVersion = 35
        ndkVersion = "25.2.9519653"
        kotlinVersion = "2.0.21"
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle:8.1.0") // Specify Gradle Plugin Version
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinVersion")
        classpath("com.google.gms:google-services:4.4.0") // 🔹 Add this for Firebase
        classpath("com.google.firebase:firebase-crashlytics-gradle:2.9.9") // 🔹 Add this for Firebase Crashlytics
        
    }
}}

apply plugin: "com.facebook.react.rootproject"
